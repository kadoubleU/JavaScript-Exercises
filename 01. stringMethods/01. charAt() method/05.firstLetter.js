function firstLetter(input) {
    let getTheWord = input[0];

    let getTheLetter = getTheWord.charAt(0);

    console.log(getTheLetter);
}

firstLetter(["hello"]);

// Write a function that takes a string and returns the first character of that string using charAt()